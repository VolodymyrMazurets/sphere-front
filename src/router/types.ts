export enum RouteTypes {
  home = '/',
  result = '/result',
  list = '/list',
  listDetails = '/list/:id',
  profile = '/profile/:id',
  favorite = '/favorite',
  help = '/help',
  search = '/search',
}
